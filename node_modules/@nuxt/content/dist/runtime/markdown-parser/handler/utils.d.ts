/**
 * Parses the value defined next to 3 back ticks
 * in a codeblock and set line-highlights or
 * filename from it
 */
export declare function parseThematicBlock(lang: string): {
    language: any;
    highlights: any;
    fileName: any;
    filename?: undefined;
} | {
    language: string;
    highlights: number[];
    filename: string;
    fileName?: undefined;
};
export declare function getTagName(value: string): string;
/**
 * Wrap `nodes` with line feeds between each entry.
 * Optionally adds line feeds at the start and end.
 */
export declare function wrap(nodes: any[], loose?: boolean): any[];
