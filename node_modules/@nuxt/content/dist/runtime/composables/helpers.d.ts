import type { NavItem } from '../types';
export declare const useContentHelpers: () => {
    navBottomLink: (link: NavItem) => any;
    navDirFromPath: (path: string, tree: NavItem[]) => any;
    navPageFromPath: (path: string, tree: NavItem[]) => any;
    navKeyFromPath: (path: string, key: string, tree: NavItem[]) => any;
};
